services:
  - type: static
    name: ai-lead-generator-frontend
    env: static
    staticPublishPath: dist
    buildCommand: npm run build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
