version: '3.8'
services:
  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: lead_user
      POSTGRES_PASSWORD: lead_pass
      POSTGRES_DB: lead_platform
    volumes:
      - db_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
volumes:
  db_data:
